void pushZerosToEnd(int arr[], int n) {
	    // code here
	    int newindex =0;
	    for(int i=0;i<n;i++)
	    {
	    if(arr[i]!=0)
	    {
	        arr[newindex] = arr[i];
	        newindex++;
	    }
	    }
	    while(newindex<n)
	    {
	        arr[newindex++] = 0;
	    }
	}
