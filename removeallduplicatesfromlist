	string removeDuplicates(string str) {
	    // code here
	    string res="";
   int freq[256]={0};
   
   for (int i =0 ; i < str.length(); i++)
   {
       if (freq[str[i]]==0)
       {
           freq [str[i]]=1;
           res+=str[i];
       }
   }
   return res;
  

	}
