int height(struct Node* node){
        // code here 
        if(node == NULL)
        {
            return 0;
        }
         int ans= max(height(node->left), height(node->right)) ;
         return ans+1;
    }
